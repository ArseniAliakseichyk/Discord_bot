# Assets

Drop the media referenced by the main `README.md` here:

- `now-playing.png` — screenshot of the "Now playing" embed with control buttons
- `demo.mp4` — short screen recording of playback / controls

Until these are added, the image/video links in the README will show as missing.
